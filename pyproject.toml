[project]
name = "pseudo"
version = "0.1.0"
description = "A simple implementation of academic psudecode"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
